
<div class="bg-white rounded-lg shadow p-10 max-w-2xl mx-auto mt-10">
	<h1 class="text-2xl font-bold mb-6">Dashboard de Configurações</h1>
	<div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
		<div class="bg-blue-100 rounded-lg p-6 text-center">
			<h2 class="text-lg font-semibold text-blue-800 mb-2">Usuários</h2>
			<p class="text-3xl font-bold text-blue-900">{{userCount}}</p>
		</div>
		<div class="bg-green-100 rounded-lg p-6 text-center">
			<h2 class="text-lg font-semibold text-green-800 mb-2">Lembretes</h2>
			<p class="text-3xl font-bold text-green-900">{{reminderCount}}</p>
		</div>
		<div class="bg-red-100 rounded-lg p-6 text-center">
			<h2 class="text-lg font-semibold text-red-800 mb-2">Expirados</h2>
			<p class="text-3xl font-bold text-red-900">{{expiredCount}}</p>
		</div>
	</div>
	<a href="/settings" class="text-blue-600 hover:underline">Voltar para Configurações</a>
</div>
