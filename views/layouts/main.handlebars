<!DOCTYPE html>
<html lang="pt-br">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Dashboard</title>
        <script src="https://cdn.tailwindcss.com"></script>
    </head>
    <body class="bg-gray-100 font-sans min-h-screen">

    <!-- Menu Topo -->
    <header class="bg-white shadow-md py-4 px-6 flex justify-between items-center">
        <h1 class="text-xl font-bold text-gray-800">Lembretes</h1>
        <nav class="space-x-4">
        <a href="#" class="text-gray-600 hover:text-blue-600">Início</a>
        <a href="#" class="text-gray-600 hover:text-blue-600">Relatórios</a>
        <a href="#" class="text-gray-600 hover:text-blue-600">Configurações</a>
        </nav>
    </header>

    <!-- Conteúdo -->
    <main class="p-6">
        <div class="bg-white rounded-lg shadow p-6">
        <h2 class="text-2xl font-semibold mb-4">Bem-vindo!</h2>
        <p class="text-gray-700">Este é o corpo do dashboard. Você pode adicionar gráficos, cards, tabelas, etc.</p>
        </div>
    </main>

    </body>
</html>